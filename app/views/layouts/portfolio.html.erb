<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="keywords" content="<%= @seo_keywords %>">

    <%= stylesheet_link_tag 'portfolio_res', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="p-3">
     <div class="container">
      <div class="wrapper">
      <%= render 'shared/blog_nav' %>
      <section class="p-3 text-center">
        <div class="container">
          <h1 class="p-3">My portfolio</h1>
          <p class="lead text-muted">In this section I have published the apps that I created.</p>

            <%= link_to 'Home', root_path, class: 'btn btn-primary my-2' %>
            <%= link_to 'Contact', contact_path, class: 'btn btn-secondary my-2' %>

        </div>
      </section>
      <%= yield %>

      <footer class="text-muted">
        <div class="text-center">
          <p class="">
            <%= render 'shared/application_footer' %>
          </p>
        </div>
      </footer>
    
      <%= source_helper %>
      </div>
    </div>
  </body>
</html>
